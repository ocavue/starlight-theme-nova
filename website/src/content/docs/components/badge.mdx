---
title: Badge
---

import BadgePreview from '../../snippets/badge.mdx'
import BadgeContent from '../../snippets/badge.mdx?raw'
import { Code } from 'starlight-theme-nova/components'

To display small pieces of information, such as a status or category, use the `<Badge>` component.

## Preview

<BadgePreview />

## Usage

<Code code={BadgeContent} lang="jsx" />

## `<Badge>` Props

The `<Badge>` component accepts the same props as Starlight's [`<Badge>` component](https://starlight.astro.build/components/badges/).
