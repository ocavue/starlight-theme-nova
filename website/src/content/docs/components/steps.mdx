---
title: Steps
---

import StepsPreview from '../../snippets/steps.mdx'
import StepsContent from '../../snippets/steps.mdx?raw'
import { Code } from 'starlight-theme-nova/components'

To style a numbered list of tasks to create step-by-step guides, use the `<Steps>` component.

## Preview

<StepsPreview />

## Usage

<Code code={StepsContent} lang="jsx" />

## `<Steps>` Props

The `<Steps>` component does not accept any props.
